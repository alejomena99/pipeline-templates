name: My Workflow

on:
  workflow_dispatch:

jobs:
  echo-message:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Echo message
        run: echo "¡Funcionó!"
